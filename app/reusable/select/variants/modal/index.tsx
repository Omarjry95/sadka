import React from 'react';
import {BaseStrictProps} from "@app/reusable/select/models";
import {View} from "react-native";

export default function Modal({  }: BaseStrictProps) {

    return (
        <View>

        </View>
    )
}