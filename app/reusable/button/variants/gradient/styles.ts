import {StyleSheet} from "react-native";

export default StyleSheet.create({
    container: {
        width: "100%"
    },
    gradience: {
        width: "100%",
        borderRadius: 10,
        alignItems: "center",
        justifyContent: "center"
    }
})