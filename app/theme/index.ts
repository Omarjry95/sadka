import {DefaultTheme, Theme} from "@react-navigation/native";

const mainTheme: Theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: "#D00027"
    },
};

export default mainTheme;